# Single-node quick start — no TLS, no RocksDB, no clustering.
# Usage: docker compose -f docker-compose.lite.yml up
services:
  cerulean:
    build:
      context: .
      dockerfile: Dockerfile.lite
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=info
      - ACL_MODE=permissive
